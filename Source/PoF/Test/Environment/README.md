# Environment functional tests

Per-system gameplay tests for the Environment system, subclassing `AARPGFunctionalTestBase`.
Authored per the matching `docs/improvements/0X-*/tests.md`. See `../README.md`.
